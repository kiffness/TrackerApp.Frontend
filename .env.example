# Base URL for API calls. Leave empty when the frontend and backend
# are served from the same domain (e.g. behind a shared nginx reverse proxy).
# Set to the full API URL if they are on separate domains.
VITE_API_URL=
